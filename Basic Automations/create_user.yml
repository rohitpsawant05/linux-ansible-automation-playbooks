---
- name: Create multiple users
  hosts: all
  become: yes
  vars:
    users:
      - devuser
      - testuser
      - deployuser
  tasks:
    - name: Create users
      user:
        name: "{{ item }}"
        state: present
      loop: "{{ users }}"
